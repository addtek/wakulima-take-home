import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 59,
    height: 59,
    borderRadius: 59,
    padding: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
